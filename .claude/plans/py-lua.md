# Plan: Convert build-self.py to Lua

**DONE** — `build-self.py` was removed. The build script is now `build.sh`,
which fetches Lua modules from GitHub and generates `index.html`. No local
Lua files or Python needed.
